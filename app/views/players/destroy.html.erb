<br />
<br />
<br />
<br />
<div class = "text-center"> 
	<%= @player.name.upcase %>
	<br />
	RANK
	<br />
	 
	<% if current_user && current_user == @player.user %>
		<%= link_to 'Delete', player_path(@player), :method => :delete, :data => {:confirm => 'Are you sure you want to delete this?'} %>
	<% else %>
		<%= link_to 'Back', players_path %>
	<% end %>
</div>

	</body>
<br />
<br />
<br />

